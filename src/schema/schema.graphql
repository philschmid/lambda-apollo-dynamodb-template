# @format

type Query {
  hello(id: Int): User
  findById(id: Int): String
}
type Mutation {
  createUser(username: String!, name: String, age: Int): User
}
type User {
  userid: String
  username: String
  name: String
  age: Int
}
